# Claude Code 官方统一 Skill 编写规范指导手册
> 用途：下发给 Claude Code 严格遵循，标准化编写、优化、重构所有开发类、工程类、运维类、项目类 Agent Skill，统一格式、逻辑、边界、执行逻辑，实现全自动可调用。

## 一、手册适用范围
本规范专为 **Claude Code** 定制，所有用于代码开发、项目运维、Git 操作、环境配置、文档排版、脚本编写、工程调试等场景的自定义 Skill，必须100%遵循本规范，保证调用稳定、语义统一、无歧义、可自动化执行。

## 二、顶层强制核心设计原则
1. **单一职责原则**
一个 Skill 仅完成**唯一确定任务**，严禁捆绑多个无关功能，拒绝大而全万能技能。
2. **场景边界绝对清晰**
明确正向使用场景 + 反向禁用场景，杜绝模糊调用、乱调用。
3. **极简上下文原则**
无冗余话术、无多余铺垫、无主观闲聊，只保留执行必需信息，节省模型上下文。
4. **指令可落地原则**
全部流程使用**命令式执行步骤**，拒绝描述性文案，可直接复制执行。
5. **低自由裁量原则**
固定流程、固定格式、固定输出范式，最大限度限制模型自主发挥空间。

## 三、Skill 基础元数据编写规范
### 1. 命名规则
- 格式：纯小写英文 + 英文连字符 `-`
- 结构：`动作-对象-业务场景`
- 示例：`git-local-commit`、`java-code-lint`、`latex-fix-two-column`
- 禁止：中文、大写字母、空格、版本号、情绪化词汇、模糊名词

### 2. 功能描述 Description 规范
- 视角：第三方客观陈述，不用第一/二人称
- 句式：本技能用于XX操作，适用于XX开发场景
- 篇幅：精简凝练，控制在200字符以内，无口语化内容

## 四、Skill 固定标准六大结构（顺序不可调换）
1. 技能基础名称与用途
2. 正向触发场景 `Use This When`
3. 反向禁用场景 `Do NOT Use When`
4. 结构化输入参数 `Input Params`
5. 标准化时序执行步骤 `Execution Steps`
6. 异常兜底失败策略 `On Failure Handling`

## 五、分模块精细化编写教程
### 5.1 触发场景编写教程
- 正向场景：写**具体用户指令、项目行为、业务场景**
- 错误写法：用户需要操作代码
- 标准写法：用户要求格式化后端Java代码、用户需要合并本地Git分支
- 禁用场景：精准排除冲突场景、未完成前置操作场景、权限不足场景

### 5.2 输入参数编写教程
- 统一格式：参数名 | 数据类型 | 必填/选填 | 功能说明
- 支持类型：文件路径、文本内容、终端命令、配置字段、版本号、布尔值
- 严禁模糊描述：相关内容、对应文件、所需资料

### 5.3 核心执行步骤编写教程（Claude Code 重点）
1. 严格按照**执行时间顺序**编排步骤
2. 优先编写最短成功执行流程，再补充边界细节
3. 开发类 Skill 优先输出**可直接运行终端命令、完整代码片段、配置内容**
4. 一句仅对应一个独立操作，不合并多项动作
5. 文件操作必须写明：绝对路径、文件名、写入内容、替换规则、保存方式
6. 代码修改明确标注修改位置、修改逻辑，保留原有合法业务逻辑

### 5.4 失败兜底策略编写教程
必须覆盖三类高频异常：
1. 文件/目录路径不存在异常
2. 终端命令执行报错异常
3. 运行环境缺失、权限不足异常

统一兜底逻辑：
- 轻微异常：告知错误原因 + 提供一键修复命令
- 严重异常：立即终止执行 + 回滚已执行操作 + 输出完整排查方案
- 禁止模型自动跳过报错、忽略异常继续执行

## 六、文件格式与篇幅统一规范
1. 单份 Skill 正文内容控制在 **500行以内**，精简高效
2. 复杂逻辑拆分：主Skill保留核心流程，细分规则存入附属文档，禁止主文件臃肿
3. 路径统一标准：全程使用 Linux 正斜杠 `/`，禁用 Windows 反斜杠 `\`
4. 专业术语全程统一，前后文案用词一致，不随意替换同义词汇

## 七、严格禁止的编写反模式
1. 禁止编写无明确答案的开放式模糊指令
2. 禁止写入固定时间、临时项目信息等时效性内容
3. 禁止加入主观建议、安抚话术、闲聊对话内容
4. 禁止设计多层链式嵌套调用，仅支持单层文件引用
5. 禁止省略环境依赖、前置准备等必要前置步骤
6. 禁止 Skill 私自篡改项目核心配置、全局环境变量

## 八、Claude Code 标准编写工作流
1. 接收需求：确定技能用途、使用场景、目标人群
2. 预设3-5条验收评测用例，确定最终交付标准
3. 套用统一空白模板搭建整体框架
4. 依次填充：触发场景 → 输入参数 → 执行步骤 → 异常策略
5. 全维度自检合规性
6. 直接输出成品 Skill 文件，无需二次人工修改

## 九、通用空白标准 Skill 模板
```markdown
# Skill Name: 遵循命名规则填写
## Skill Purpose
填写精简客观的技能核心用途

## Use This Skill When
1. 具体可触发使用场景1
2. 具体可触发使用场景2

## Do NOT Use This Skill When
1. 禁止调用场景1
2. 禁止调用场景2

## Input Params
1. file_path: 字符串路径 | 必填 | 项目目标文件绝对路径
2. run_env: 字符串 | 选填 | 指定运行环境版本

## Execution Steps
1. 执行前置环境校验
2. 执行核心业务操作命令
3. 执行结果自检校验
4. 执行收尾保存/同步操作

## On Failure Handling
1. 路径错误：提示标准路径书写格式
2. 命令执行失败：输出完整排错指令
3. 环境缺失：提供一键安装依赖脚本